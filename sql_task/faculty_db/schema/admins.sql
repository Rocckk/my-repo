-- table structure for the table `administrative_roles`

create table `administrative_roles`(
`id` int unsigned not null auto_increment,
`role_name` varchar (70),
primary key (`id`)
)
default charset utf8
;
